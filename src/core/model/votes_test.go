package model

import (
	"fmt"
	"testing"
	//"unicontract/src/common"
)

func Test_Votes(t *testing.T) {
	//create new obj
	var str interface{}
	str = "334"
	a, ok := str.(string)
	fmt.Println(ok)
	fmt.Println(a)
}
